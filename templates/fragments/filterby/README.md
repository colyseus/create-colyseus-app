### filterBy

`.filterBy(["mode"])` on the room definition splits matchmaking by the `mode` join option, so
players asking for different modes never land in the same room.

- https://docs.colyseus.io/matchmaker
